import React from 'react';

export default function DocSubTitleContain({text}) {
    return (
        <div className="doc-subTitle-container">
            <h3 className="doc-subTitle-text">{text}</h3>
        </div>
    )
}